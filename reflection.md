# Reflection cá nhân Bùi Văn Khởi

- **Vai trò:** Evaluation.
- **Phần tôi làm:** Tham gia xây dựng luồng RAG, tích hợp Gemini và chạy golden set đánh giá hệ thống.
- **AI hỗ trợ:** AI hỗ trợ gợi ý cấu trúc code, rà soát prompt và phân tích log lỗi. Tôi trực tiếp kiểm tra retrieval, luồng API và kết quả từng case trước khi sử dụng.
- **Bài học:** Lượt eval đầu chỉ đạt **10%** do lỗi parser, quota và timeout cho thấy một tính năng AI chạy được chưa đồng nghĩa với đủ tin cậy. Hệ thống AI cần logging, fallback và bộ kiểm thử rõ ràng để biết lỗi nằm ở retrieval, model hay phần tích hợp.

